defmodule Waveform.Music.ChordTest do
  use ExUnit.Case

  # alias Waveform.Music.Chord, as: Subject

  test "returns notes for all chord types" do
  end

  test "returns notes for chord inversions" do
  end

  test "returns sub 5" do
    # Subect.notes(%Subject.Roman{roman: :ii, key: :c, octave: 5})
  end
end
